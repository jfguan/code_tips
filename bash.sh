Search for previous command:
    CTRL-R
!$ Take the last thing from the previous line 
!! Take the last line

*pattern: all files that end in pattern, asdofpattern, sdf_pattern etc.

echo "some text to insert" >> filename
cat files: show file contents
cat > file: overwrite files with stuff
cat >> file: append

head <file> 

rm -rf directory
--recursive, --force

grep

Unpack tarball
    tar -xf filename

chmod u+x program_name
/usr/local/bin/zsh